name: Node CI

on: [push]

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - uses: actions/setup-node@v1
      with:
        node-version: '12.x'
    #- run: npx ipfs-deploy 
    - run: echo "listing folder"
    - run: ls
    - run: echo "listing partent"
    - run: ls ../ 
